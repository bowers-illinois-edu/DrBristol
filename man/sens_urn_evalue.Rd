% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evalues.R
\name{sens_urn_evalue}
\alias{sens_urn_evalue}
\title{Sensitivity analysis in the e-value scale}
\usage{
sens_urn_evalue(
  obs_support,
  total_obs,
  rival_obs = NULL,
  weights = NULL,
  e_threshold
)
}
\arguments{
\item{obs_support}{An integer representing the number of observations
in favor of the working hypothesis. Must be less than or equal to the total.}

\item{total_obs}{An integer representing the total number of observations}

\item{rival_obs}{Optional. The number of observations in the urn that do not support the working theory.}

\item{weights}{A vector of numeric weights representing the differential
evidentiary weight of the working theory supporting observations.}

\item{e_threshold}{Numeric scalar \eqn{> 0}. Target e-value at which
to assess sensitivity.}
}
\value{
Whatever \code{\link[=sens_obs_two_types]{sens_obs_two_types()}} returns (typically a list including the
odds-ratio \eqn{\omega}); the only difference is that you parameterize
the problem by an e-value threshold rather than a p-value threshold.
}
\description{
Wrapper around \code{\link[=sens_obs_two_types]{sens_obs_two_types()}} that works on the e-value scale. For a
given e-value threshold \eqn{e_\star}, this function finds the odds
ratio \eqn{\omega} at which the conservative e-value
(as returned by \code{\link[=find_e_two_types]{find_e_two_types()}}) would be equal to \eqn{e_\star}.

Because the underlying calibration is via \eqn{p_{\max}} and
\code{\link[=e_from_p]{e_from_p()}}, this is equivalent to asking for the odds ratio at which
the p-value upper bound would be \eqn{\alpha_\star = 1 / e_\star},
and then calling \code{\link[=sens_obs_two_types]{sens_obs_two_types()}} with \code{p_threshold = alpha_star}. \href{file-service://file-QZ1mjQ3FjfNcsuGnfUiwoe}{oai_citation:5‡p_value_process_tracing.pdf}
}
\examples{
# Suppose we observed 7 supportive pieces of evidence out of 10.
# How large must the observation bias be for the e-value to drop to ~2
# (i.e. roughly p >= 0.5)?
sens_urn_evalue(obs_support = 7, total_obs = 10, e_threshold = 2)

}
