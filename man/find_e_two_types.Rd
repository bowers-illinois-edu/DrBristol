% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evalues.R
\name{find_e_two_types}
\alias{find_e_two_types}
\title{E-values for the +1 hypergeometric urn model}
\usage{
find_e_two_types(
  obs_support,
  total_obs,
  weights = NULL,
  odds = 1,
  method = c("inverse", "kappa"),
  kappa = 0.5,
  cap = Inf,
  interpretation = FALSE
)
}
\arguments{
\item{obs_support}{An integer representing the number of observations in
favor of the working hypothesis. Must be less than or equal to \code{total_obs}.}

\item{total_obs}{An integer representing the total number of observations
made. It can be greater than or equal to the \code{obs_support}.}

\item{weights}{Double vector. Default is equal weight for each observation when
weights=NULL is \code{rep(1,obs_support)}. To indicate that one observation
should have twice the weight of any other one might use
\code{rep(c(2,1),c(1,obs_support-1))}}

\item{odds}{The odds of observing a rival versus working-theory observation.
This can be interpreted as "bias" in observation. Or "relative ease" of observation.}

\item{method}{Character; passed to \code{\link[=e_from_p]{e_from_p()}} (either \code{"inverse"}
or \code{"kappa"}).}

\item{kappa}{Numeric; κ parameter for the \code{"kappa"} method.}

\item{cap}{Upper cap for the returned e-value; passed to
\code{\link[=e_from_p]{e_from_p()}}.}

\item{interpretation}{Logical. If \code{FALSE} (default), return a
numeric e-value. If \code{TRUE}, return a list with the e-value and
a human-readable character string summarizing the result.}
}
\value{
If \code{interpretation = FALSE}, a single numeric e-value.
If \code{interpretation = TRUE}, a list with components:
\itemize{
\item \code{e}: the e-value (numeric scalar).
\item \code{interp}: a character string describing the result.
}
}
\description{
Compute e-values for the DrBristol +1 hypergeometric urn model by
first obtaining the conservative upper bound on the p-value under the
rival theory and then calibrating that p-value into an e-value.

Concretely, this function:
\enumerate{
\item Calls \code{\link[=find_p_two_types]{find_p_two_types()}} with \code{interpretation = FALSE}
to obtain the upper bound \eqn{p_{\max}} on the p-value under the
+1 urn model. \href{file-service://file-QZ1mjQ3FjfNcsuGnfUiwoe}{oai_citation:4‡p_value_process_tracing.pdf}
\item Applies \code{\link[=e_from_p]{e_from_p()}} to \eqn{p_{\max}} using the chosen
calibration (inverse or κ-calibrator).
}

Because \eqn{p_{\max}} is conservative, the resulting e-value is
likewise conservative: it is the \emph{minimum} e-value consistent with
the +1 urn model and the chosen calibrator.
}
\examples{
# Basic: 7 supportive observations out of 10 total
find_e_two_types(obs_support = 7, total_obs = 10)

# With textual interpretation
res <- find_e_two_types(
    obs_support = 7, total_obs = 10,
    interpretation = TRUE
)
res$e
res$interp

# Using κ-calibration
find_e_two_types(
    obs_support = 7, total_obs = 10,
    method = "kappa", kappa = 0.5
)

}
\seealso{
\code{\link[=find_p_two_types]{find_p_two_types()}}, \code{\link[=e_from_p]{e_from_p()}}, \code{\link[=sens_urn_evalue]{sens_urn_evalue()}}
}
